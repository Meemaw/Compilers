
class Program {

    /* This is a simple test program */
    // single line comment

    int i, j;
    real number;
    static void main() {
        real array_[];
        i = 0;
        for (int x=10; !x; x--)
            if (x == 5)
                break;
            else
                continue;

        if (i-- + number - 5 * 10 / 50 == 50 % 2) {
            if (50 > 40 && 30 < 50 || 70 != 69)
                if (i <= number && number++ * 5 >= i)
                    number = 12.34 + 50 + 50E20 + 50.50E20;
                else
                    return;
        }
    }
}

